settings:
  kubeContext: "do-ams3-thesis"

namespaces:
  postgresql-ha:
    protected: false

helmRepos:
  bitnami: "https://charts.bitnami.com/bitnami"

apps:
  postgresql-ha:
    namespace: "postgresql-ha"
    enabled: true
    chart: "bitnami/postgresql-ha"
    version: "3.2.7"
    valuesFile: "postgresql-ha/values.yaml"

