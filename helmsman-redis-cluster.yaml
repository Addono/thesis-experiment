settings:
  kubeContext: "do-ams3-thesis"

namespaces:
  redis-cluster:
    protected: false

helmRepos:
  bitnami: "https://charts.bitnami.com/bitnami"

apps:
  redis:
     namespace: "redis-cluster"
     enabled: true
     chart: "bitnami/redis-cluster"
     version: "3.1.10"
     valuesFile: "redis-cluster/values.yaml"

